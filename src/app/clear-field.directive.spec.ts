import { ClearFieldDirective } from './clear-field.directive';

describe('ClearFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearFieldDirective();
    expect(directive).toBeTruthy();
  });
});
